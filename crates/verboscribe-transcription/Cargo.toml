[package]
name = "verboscribe-transcription"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
verboscribe-core = { path = "../verboscribe-core" }

[dev-dependencies]
tempfile = "3"
