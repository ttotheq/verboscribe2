[package]
name = "verboscribe-audio"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
hound = "3.5"
thiserror = "2"
verboscribe-core = { path = "../verboscribe-core" }

[dev-dependencies]
tempfile = "3"
