[package]
name = "verboscribe-storage"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
verboscribe-core = { path = "../verboscribe-core" }

[dev-dependencies]
tempfile = "3"
